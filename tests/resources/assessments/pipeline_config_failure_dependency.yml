name: ExamplePipeline
version: "1.0"
# Value replaced prior to actual use:
extract_folder: /replaced/after/loading/
steps:
  - name: package_status
    type: python
    # Relative to the tests/resources/ directory in the project.
    extract_source: assessments/db_extract_dep.py
    mode: overwrite
    frequency: daily
    flag: active
    dependencies:
      - databricks_labs_ucx>=0.50.0


